[project]
name = "manga-reader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask",
    "flask-caching",
    "python-dotenv",
]
